include ../mixins/horizontalLine.pug

- let getLocation = function() {
-   let location = '主页'
-   if (is_page()) location = page.title
-   if (is_post()) location = page.title
-   if (is_archive()) location = '时间轴'
-   if (is_year()) location += ': ' + page.year
-   if (is_month()) location += ': ' + page.month + '/' + page.year
-   if (is_tag()) location = '标签: ' + page.tag
-   if (is_category()) location = '分类: ' + page.category
-   return location
- }

.location-bar
  .line-1
    +horizontalLine('3px')
  .line-2
    +horizontalLine('1px')
  p.text= getLocation()
  include ./switch-button
  .line-3
    +horizontalLine('1px')
